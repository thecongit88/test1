workflows:
  ios-project-debug: # workflow ID
    name: iOS debug # workflow name
    scripts:
     - flutter pub get
     - flutter build ios --debug --no-codesign
    publishing:
      email:
        recipients:
          - thecongit88@gmail.com